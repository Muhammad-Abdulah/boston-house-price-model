# boston-house-price-model
End-to-end ML regression project predicting Boston housing prices using Random Forest. Includes preprocessing, feature selection, model training, evaluation (R², RMSE), and model persistence using joblib.
